# SupportHub Docker Environment
# Use this for Docker deployments to avoid formatting issues

NODE_ENV=production
PORT=5000
DATABASE_URL=postgresql://supporthub:secure_password_123@db:5432/supporthub?sslmode=disable
SESSION_SECRET=your-very-long-random-session-secret-change-this-in-production
TRUST_PROXY=1
DB_PASSWORD=secure_password_123

# Optional email settings
SENDGRID_API_KEY=your_sendgrid_api_key_here
VERIFIED_SENDER_EMAIL=noreply@yourdomain.com
