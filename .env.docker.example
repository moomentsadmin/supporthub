# ============================================
# SupportHub Production Environment Variables
# ============================================

# Domain & SSL Configuration (for HTTPS deployment with Let's Encrypt)
DOMAIN=yourdomain.com
EMAIL=admin@yourdomain.com

# Database Configuration
POSTGRES_DB=supporthub
POSTGRES_USER=supporthub
POSTGRES_PASSWORD=change_this_to_strong_password

# Application Configuration
NODE_ENV=production
PORT=5000
SESSION_SECRET=change_this_to_random_secret_min_32_chars

# Email Configuration (SendGrid - Optional)
SENDGRID_API_KEY=
VERIFIED_SENDER_EMAIL=

# SMS Configuration (Twilio - Optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ============================================
# Security Notes:
# - Generate SESSION_SECRET: openssl rand -hex 32
# - Generate POSTGRES_PASSWORD: openssl rand -base64 24
# - Update DOMAIN to your actual domain
# - Update EMAIL for Let's Encrypt notifications
# ============================================
